#include <stdio.h>
/**
 *main - print num base 16
 *
 *Return: Always 0 (succes)
 */
int main(void)
{
	int n, s, num = '0';
	char abc = 'a';

	s = '\n';
	n = 10;
	while (n != 0)
	{
		putchar(i);
		num++;
		n--;
	}
	n = 6;
	while (n != 0)
	{
		putchar(abc);
		n--;
		abc++;
	}
	putchar(s);
	return (0);
}
